// CommonJS, Every file is common (by default)
// Module - Encapsulated Code (only share minimum)
const names = require('./004.Name');
const sayHi = require('./005.Utils');

console.log()
console.log(names);


sayHi('[MT]');

// sayHi('Susan');
// sayHi(john);
// sayHi(peter);
